# 👥 역할 분담과 일정: 누가 뭐 했고, 뭐 해야 하나?

**승규님 + 준배님 협업 가이드**

---

## 🎯 1. 현재까지 한 일 (Day 1 완료!)

### ✅ 승규님이 한 일 (80% 완료!)

```
┌─────────────────────────────────────┐
│ 🧠 로직 설계 & 구현 (승규님)        │
└─────────────────────────────────────┘

✅ 점수 계산 로직 설계
   - 어떤 메트릭 쓸지 (에러율, 레이턴시, CPU)
   - 가중치 결정 (50%, 35%, 15%)
   - 점수 구간 설정 (0-30-70-100)

✅ Python 코드 구현
   - score.py (550줄)
   - 완벽하게 작동함!
   - 테스트 50개 통과

✅ Fallback 시스템
   - Anomaly Detection 없을 때 대비
   - 임계값 설정 (1%, 5%, 10% 등)

✅ 문서 작성
   - API 스펙
   - Java 이식 가이드
   - 팀원 공유 문서

→ 승규님 파트는 거의 완성!
→ 이제 "연결"만 하면 됨!
```

---

### ❓ 준배님이 할 것 (아직 시작 전)

```
┌─────────────────────────────────────┐
│ ☁️ AWS 인프라 (준배님)              │
└─────────────────────────────────────┘

⬜ Terraform으로 인프라 구축
   - EC2 / ALB / VPC / Security Group
   - 배포 파이프라인

⬜ CloudWatch 설정
   - 메트릭 수집 설정
   - Alarm 3개 생성
   - SNS Topic 연결

⬜ 모니터링 대상 생성
   - 실제 서버 띄우기
   - 메트릭 발생시키기

→ 준배님은 인프라 담당
→ 승규님과 협업 필요!
```

---

## 📍 2. 경계선: 어디까지가 내 일?

### 승규님 영역 (완료!) ✅

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 데이터 분석 & 점수 계산
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

입력:
{
  "error_rate": 2.5,
  "latency": 450,
  "cpu": 65
}

↓ 승규님 로직

출력:
{
  "health_score": 45,
  "health_state": "warning",
  "coach_message": "⚠️ 주의!"
}

이 로직은 이미 완성!
```

### 준배님 영역 (예정) ⬜

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
☁️ AWS 인프라 & 데이터 수집
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

CloudWatch:
- 메트릭 수집
- Alarm 생성
- SNS 전송

↓ 준배님 인프라

Backend로 전달:
{
  "error_rate": 2.5,
  "latency": 450,
  "cpu": 65
}

이 데이터를 만드는 게 준배님 일!
```

---

## 🤝 3. 협업 지점: 어디서 만나나?

```
전준배님 (AWS)          이승규님 (로직)          Backend팀 (Java)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

CloudWatch           →   승규님 명세서    →    Java 구현
Alarm 생성                - API 형식           - SNS 수신
SNS 전송                  - 계산 로직          - 점수 계산
                          - 예시 코드          - DB 저장

│                         │                     │
│                         │                     │
└────── 메트릭 데이터 ────┴───── 점수 계산 ─────┘
```

### 협업 필요 시점:

#### 협업 1: CloudWatch Alarm 설정 (내일 오전)
```
시간: Day 2 오전 (9:00 - 10:30)

준배님:
□ Alarm 3개 생성
  - penguin-land-error-rate
  - penguin-land-latency
  - penguin-land-cpu

승규님 역할:
☑️ 임계값 알려주기 (이미 문서화됨!)
   - 에러율: 1%, 5%, 10%
   - 레이턴시: 300ms, 700ms, 1500ms
   - CPU: 50%, 80%, 95%

☑️ Anomaly Detection Band 설정 확인
   - 올바르게 설정됐는지 검토

승규님이 할 일:
→ 준배님한테 "03_API명세서.md" 공유
→ 임계값 설명
→ 테스트 도와주기
```

---

#### 협업 2: SNS 메시지 형식 확인 (내일 오전)
```
시간: Day 2 오전 (10:30 - 11:30)

준배님:
□ SNS에서 오는 메시지 형식 확인
□ Backend팀한테 전달

승규님 역할:
☑️ 메시지 파싱 방법 알려주기
   (이미 API명세서에 있음!)

예시:
{
  "AlarmName": "penguin-land-latency",
  "NewStateReason": "Threshold Crossed: [850.0]",
  "Trigger": {
    "MetricName": "TargetResponseTime"
  }
}

승규님이 할 일:
→ Backend팀한테 파싱 로직 설명
→ Java 코드 예시 제공 (이미 작성됨!)
```

---

#### 협업 3: 통합 테스트 (내일 오후)
```
시간: Day 2 오후 (15:00 - 17:00)

준배님:
□ 실제 Alarm 발생시키기
□ SNS 전송 확인

승규님 역할:
☑️ 점수가 예상대로 나오는지 확인
☑️ 이상한 값 나오면 디버깅

테스트 시나리오:
1. 정상 상태 → 0~30점 나와야 함
2. 에러율 5% → 40~60점 나와야 함
3. 에러율 10% → 70~100점 나와야 함

승규님이 할 일:
→ 예상 점수 계산해서 미리 알려주기
→ 실제 점수와 비교
→ 차이 나면 원인 분석
```

---

## 📅 4. 내일(Day 2) 상세 일정

### 🌅 오전 (09:00 - 12:00)

#### 09:00 - 09:30: 킥오프 미팅
```
참석: 승규님 + 준배님 + Backend팀

승규님 발표:
□ Python 로직 시연
  - "이렇게 계산돼요!" 보여주기
  - 테스트 케이스 실행

□ 협업 포인트 설명
  - CloudWatch 설정 요청사항
  - SNS 메시지 형식
  - Backend 구현 가이드

예상 시간: 30분
```

---

#### 09:30 - 11:00: CloudWatch 설정 (준배님과 협업)
```
승규님 역할:

□ 09:30 - 10:00: 임계값 설명
  → 준배님이 Alarm 생성하는 동안
  → 옆에서 "이 값은 왜 이렇게 설정했는지" 설명

□ 10:00 - 10:30: Anomaly Detection 설정 확인
  → 준배님이 설정한 거 검토
  → "Band 폭이 적절한지" 확인

□ 10:30 - 11:00: 테스트 Alarm 발생
  → 수동으로 Alarm 상태 변경
  → SNS 메시지 잘 오는지 확인

준비물:
✅ 03_API명세서.md (CloudWatch 섹션)
✅ 노트북 (Python 코드 실행 준비)
✅ 계산기 (예상 점수 계산)
```

---

#### 11:00 - 12:00: Backend 코드 리뷰
```
승규님 역할:

□ Backend팀이 Java로 이식한 코드 검토

체크 포인트:
✅ calculate_severity() 로직 동일한가?
✅ 가중치 적용 맞나?
✅ 경계값 처리 맞나?

실제 테스트:
Python 결과 vs Java 결과 비교

예시:
입력: error_rate=2.5, latency=450, cpu=65
Python: 45점
Java: 45점 (동일해야 함!)

다르면:
→ 어느 단계에서 차이 나는지 찾기
→ 중간값 로그 출력해서 비교
```

---

### 🌆 오후 (13:00 - 18:00)

#### 13:00 - 14:00: 점심 + 문서 정리
```
승규님 할 일:

□ 오전에 발견한 이슈 문서화
□ Backend팀 질문 정리
□ 오후 테스트 시나리오 준비

테스트 시나리오 예시:
┌────────────────────────────────┐
│ 시나리오 1: 정상 → 주의         │
│ 시나리오 2: 주의 → 위험         │
│ 시나리오 3: 위험 → 정상         │
└────────────────────────────────┘

각 시나리오별 예상 점수 미리 계산
```

---

#### 14:00 - 15:00: DynamoDB 연동 검증
```
승규님 역할:

□ Backend가 DynamoDB에 저장한 데이터 확인

확인 항목:
✅ health_score 제대로 저장됐나?
✅ timestamp 정확한가?
✅ metrics 데이터 전부 들어갔나?

AWS Console 접속:
→ DynamoDB 테이블 확인
→ 데이터 형식 검증
```

---

#### 15:00 - 17:00: 통합 테스트 (가장 중요!)
```
승규님 역할: 테스트 리드!

테스트 1: 수동 Alarm (15:00 - 15:30)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
□ 준배님: Alarm 상태 ALARM으로 변경
□ 승규님: Backend 로그 확인
□ 예상 점수와 실제 점수 비교
□ 펭귄 애니메이션 변경 확인

테스트 2: 자동 Alarm (15:30 - 16:00)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
□ 준배님: 실제 서버 부하 발생
□ CloudWatch가 자동으로 감지
□ Alarm 자동 발동 확인

테스트 3: End-to-End (16:00 - 17:00)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
□ 배포 시작
□ CloudWatch 감시
□ Alarm 발생
□ 점수 계산
□ Frontend 펭귄 변경
□ 전체 흐름 검증!
```

---

#### 17:00 - 18:00: 버그 수정 & 최종 점검
```
승규님 역할:

발견된 이슈들 수정
□ 점수 계산 이상한 부분
□ 경계값 처리 오류
□ 메시지 한글 깨짐 등

최종 체크리스트:
✅ Alarm 3개 모두 작동
✅ 점수 계산 정확
✅ DynamoDB 저장 확인
✅ Frontend 연동 확인
✅ 펭귄 애니메이션 작동

→ 18:00 목표: "진짜 작동하는" 시스템!
```

---

## 💡 5. 승규님 핵심 역할 (내일)

### 실제로 해야 하는 것:

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. 컨설턴트 역할 (오전)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

준배님한테:
→ "CloudWatch는 이렇게 설정해야 해요"
→ "Alarm 임계값은 이 값으로요"
→ "Anomaly Detection은 이렇게요"

Backend팀한테:
→ "점수 계산은 이렇게 해요"
→ "Java 코드는 이렇게 이식하면 돼요"
→ "이 부분 주의하세요"

→ 설명하고 가이드하기!
```

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2. QA 역할 (오후)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

테스트:
→ "이 입력이면 45점 나와야 해요"
→ "실제로 45점 나왔나요?"
→ "안 나왔으면 왜 안 나왔을까요?"

디버깅:
→ "중간값 확인해봐요"
→ "이 단계에서 계산이 틀렸네요"
→ "이렇게 고치면 될 거예요"

→ 검증하고 고치기!
```

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
3. 문서화 역할 (저녁)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

하루 결과 정리:
→ "오늘 이것들 완성했어요"
→ "내일은 이것들 해야 해요"
→ "이런 문제 발견했어요"

→ 기록 남기기!
```

---

## 🚫 6. 승규님이 안 해도 되는 것

### 직접 할 필요 없어요:

```
❌ Terraform 코드 작성
   → 준배님이 함

❌ EC2 서버 설정
   → 준배님이 함

❌ Java Backend 코드 작성
   → Backend팀이 함
   → 승규님은 "검토만" 하면 됨

❌ Frontend React 코드
   → Frontend팀이 함
   → 승규님은 "API 확인만" 하면 됨

❌ DynamoDB 테이블 생성
   → 준배님이 함
   → 승규님은 "스키마 제공" 이미 했음
```

---

## 📊 7. 진행 상황 체크리스트

### 승규님 파트 (Day 1)
```
✅ 로직 설계 완료
✅ Python 코드 작성
✅ 테스트 50개 통과
✅ 문서 200페이지 작성
✅ API 스펙 작성
✅ Java 이식 가이드 작성

진행률: 80% ✅
```

### 준배님 파트 (Day 1)
```
⬜ Terraform 인프라
⬜ CloudWatch 설정
⬜ Alarm 생성
⬜ SNS 연결
⬜ 모니터링 대상 생성

진행률: 0% (내일 시작!)
```

### Backend팀 (Day 1)
```
⬜ SNS Webhook 구현
⬜ Java 로직 이식
⬜ DynamoDB 연동
⬜ API 엔드포인트 생성

진행률: 0% (내일 시작!)
```

---

## 🎯 8. 내일 성공 기준

### 최소 목표 (Must Have)
```
✅ CloudWatch Alarm 3개 작동
✅ SNS → Backend 연결 성공
✅ 점수 계산 1회라도 성공
✅ DynamoDB에 데이터 저장

→ 이것만 되면 시연 가능!
```

### 이상적 목표 (Nice to Have)
```
✅ End-to-End 전체 흐름 작동
✅ 자동 Alarm 발동 확인
✅ Frontend 펭귄 애니메이션
✅ 실시간 폴링 작동

→ 완벽한 데모!
```

---

## 💪 9. 승규님 준비물 (내일 챙기기!)

### 필수:
```
□ 노트북 + 충전기
□ 03_API명세서.md (출력 or PDF)
□ 계산기 (예상 점수 계산용)
□ 테스트 시나리오 메모
```

### 선택:
```
□ AWS 계정 접근 권한 (준배님한테 요청)
□ Backend 코드 저장소 접근 권한
□ 카페인 (길 거예요! ☕)
```

---

## 🗣️ 10. 팀원들한테 공유할 내용

### 준배님한테 (오늘 밤 or 내일 아침)
```
안녕하세요 준배님!

내일 CloudWatch 설정할 때 이 값들로 해주시면 돼요:

Alarm 1: Error Rate
- Metric: HTTPCode_Target_5XX_Count
- Threshold: Anomaly Detection
- Fallback: 5% (참고용)

Alarm 2: Latency
- Metric: TargetResponseTime (p90)
- Threshold: Anomaly Detection
- Fallback: 700ms (참고용)

Alarm 3: CPU
- Metric: CPUUtilization
- Threshold: 80%

자세한 내용은 "03_API명세서.md" 참고해주세요!
내일 오전 9시 반에 같이 설정할까요?
```

---

### Backend팀한테
```
안녕하세요!

내일 Java로 로직 이식할 때 참고하시면 돼요:

1. score.py 파일: 점수 계산 로직
2. 03_API명세서.md: Java 구현 예시 포함
3. 테스트 케이스: test_score.py 참고

특히 주의할 점:
- Anomaly Detection Band 우선 사용
- 없으면 Fallback 임계값 사용
- 가중치: 50%, 35%, 15% (순서 중요!)

질문 있으면 언제든 연락주세요!
```

---

## 🎉 요약

### 승규님은:
```
✅ 이미 80% 완성했어요!
✅ 내일은 "컨설턴트 + QA" 역할
✅ 코드 새로 짤 필요 없어요
✅ 설명하고, 검증하고, 도와주면 됨!
```

### 준배님은:
```
⬜ 내일 AWS 인프라 구축
⬜ CloudWatch + SNS 설정
⬜ 승규님과 협업 필요
```

### 내일 핵심:
```
오전: CloudWatch 설정 (준배님과)
오후: 통합 테스트 (전체 팀)
목표: "한 번이라도" 전체 흐름 성공!
```

---

**승규님 파트는 거의 다 끝났어요!** 🎉
**내일은 협업하고 검증하는 날이에요!** 💪
**화이팅!** 🚀
